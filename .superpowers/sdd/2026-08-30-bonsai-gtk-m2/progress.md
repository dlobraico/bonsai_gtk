# SDD ledger — plan: docs/superpowers/plans/2026-08-29-bonsai-gtk-m2.md
Branch m2 from main 86224d9; plan committed as the first commit. Controller rulings on the author's notes (2026-08-30): all five accepted — Task 14 writes the three nullable-binding fork patches from scratch (generator vs stub decided first, never pushed); CI pass (Task 16) after docs (Task 15); synthetic click/key delivery risk stays visible (implementer records which option they got); Attr.Name.all + constructor-time key check kept; list-box rows auto-wrapped (attrs-on-the-child), no Node.list_box_row.

## Pre-flight scan (delegated, sonnet) — findings and rulings
Pre-flight (report preflight-report.md): 12/12 checklist items verified; 4 corrections recorded at the top of the plan (commit above): no synthetic key/click in the binding → plumbing-only live tests + headless handler proof; Task 4 sample must present its window; Widget.observe_controllers for attach/detach counts; batch ~80ns/call → batch_if stays.

Task 1: complete (09ee6f7 + 1daa1b5, re-review Approved). Rulings: seal is `type t = private Private.t` (plan's spelling doesn't typecheck; alias form was doc-only) — downstream cannot match or construct raw constructors, proven by scratch-module compile errors; Patcher.mount asserts every event attr has a slot (ungated); Kind.t derives variants and both all_kinds lists assert against Kind.Variants.descriptions. Carry: Level_bar arm in Events.for_kind arrives with Task 10; the two all_kinds lists are still duplicated (count-checked).

Task 2: complete (ee64cc6, review Approved, no Important). Deviations accepted: Reconcile.diff takes a trailing unit (warning 16); unordered Updates indexed by `from` (apply-equality holds as a set only); enqueue_fixups ~path ~widget ~interest; Move on a container without `move` raises. batch measured ~80ns/call, batch_if kept. Carry to Task 3: task-2-review.md Minor M1 (one-line test change) and M3 (spec amendment); M2 ruled "leave it".

Task 3: complete (b458449 + a9b7b34 + 3ea4594, re-review Approved). Rulings: placement table lives in vtree/placement.ml and both the patcher and the headless handle read it (same principle as Events); ~visible_child naming a page that never exists raises from the fixup pass (empty-stack carve-out documented); w_entry reassert compares against the max_length-truncated text (characters) so truncation doesn't cause a per-frame write; stack-name swap fixed by pass-level give-ups-then-takes. Task 2 carries M1/M3 landed here.

Task 4: complete (9c081e5, ci.sh all green). Testing options obtained: **click = option (c)** (plumbing only -- no GdkEvent constructor, no argument-carrying emission, no gtk_test_* binding; re-verified), strengthened with a read-back of the live gesture's button/phase and with the Payload trampoline's three declined paths exercised against the callback connect_all built; the click *handler* is proved headlessly via Bonsai_gtk_test.Action.Click_at. **focus = option (a)**, genuinely end to end (grab_focus on a presented window, main loop drained), including the in_patch guard and that a removed controller stops firing. Rulings/deviations: is_event stays true for controller attrs and Events.is_controller_attr is the second, exhaustive predicate (require_slots skips them, is_supported short-circuits); Controllers names every controller it attaches (bonsai_gtk.click/focus) because a GtkButton has three of its own, so observe_controllers is counted by name not by total; sync is polymorphic in the controller type (no downcast); release split into clear+release for Patcher.disarm; live_signals.ml gained the Payload trampoline test; test_events.ml's coverage test was rewritten rather than re-promoted. End-to-end click/key gap recorded in docs/m1-backlog.md with its closing condition. Carries: README Limitations sentence Attr.on_click's doc promises (Task 15); gallery Input section should exercise Attr.on_click (Task 15/16); Controllers.attached_count's option list is hand-maintained.

Task 4 fix round 1: complete (328ca80, ci.sh all green). C1 accepted without argument -- set_static_name stores the pointer uncopied and the name passed was a runtime concat, so GTK pointed into reclaimed heap; fixed with set_name (g_strdup), and the hoisted-literal alternative is refused in a comment (OCaml does not promise literals are static, and bytecode moves them). Heap-churn regression test runs first in live_controllers.ml and reproduces the old bug exactly when the call is put back (before gc names present / after gc names empty, total unchanged -- i.e. attached but unnameable); `after destroy` is no longer vacuous (a total= column that never reads get_name, plus a positive read on the same widget before the destroy). I1 accepted: Events.Family.t + Events.controller_family (exhaustive, no wildcard) is now the single table; is_controller_attr and family_attrs derive from it and Controllers.update/clear/release/attached_count all dispatch on Family.t exhaustively, so Task 5 adding Key is four compile errors and attached_count's hand-maintained list (the previous carry) is gone. Live sweep mounts a node per controller attr and asserts one of ours appears, with the row list assert-checked against Attr.Name.all. Declined the ruling's "or better, in require_slots" placement, with reasons, offered to move it. Minors M1/M2/M3/M5 fixed; M4 (Click_event doc claims untestable in M2) and M6 (controller GClosure roots the widget; needs connect_all's signature) argued in the report.

Task 4 fix round 2: complete (93c819c, ci.sh all green). N1 accepted -- round 1's whole-widget [clear t] inside sync's per-family removal branch wiped the slots of every family already processed in the same update loop. Correction to the review's framing: it bit on the SHIPPED order too, on a line already in the golden (`focus attrs dropped, click back` printed armed=() once introspection existed), not only under a flipped Family.all. Fix does both the ruling and the reviewer's suggestion: sync's removal branch clears its own family only, AND a single clear t is hoisted to the top of Controllers.update -- branch-only would fix N1 but drop the no-sibling-armed-during-remove_controller invariant round 1 added on the reviewer's own Minor 2; offered to delete the hoist if the lead prefers the literal ruling. New Signals.armed / Controllers.armed (slot introspection) is how it is asserted at all, since no click can be synthesised: every controller line in live_controllers.ml now prints armed=. New n1 block asserts both directions (click family dropped -> focus fires in the SAME frame as the patch; focus family dropped -> click slot still armed), with baseline and post-patch focus rounds printing identically. Ordering-independence verified by experiment: flipping Family.t gives identical output with the fix, and with round-1 code the victim simply moves (shipped: click slot; flipped: focus slot + empty drain). Carry for Task 5: Family.all gains Key, which moves the loop order again -- n1 should gain a third direction then.


Task 4: complete (9c081e5 + 328ca80 + 93c819c, re-review 2 Approved). Testing options obtained: focus end-to-end (presented window, real grab_focus); click plumbing-only (observe_controllers by name, gesture settings read back, headless Click_at, Payload trampoline's three declined paths exercised directly) — end-to-end click/key gap on the backlog with its closing condition (fork patch exposing a GdkEvent constructor or gtk_test_widget_click). Critical fixed: controller names now via Event_controller.set_name (set_static_name on a computed OCaml string was a dangling pointer; GC-churn regression test). Important fixed: Events.controller_family is the single exhaustive table (is_controller_attr derives from it; Controllers.update dispatches on it). Round-2 N1: slots emptied once before any family sync (dropping one family no longer disarmed earlier families for the frame). Carries: README Limitations, gallery Input section, focus ~phase asymmetry (see task-4-review.md).

Task 5: complete (7ba161a, review Approved, no Critical/Important). Handled→GDK_EVENT_STOP chain pinned by two tests (mutation-verified by the reviewer). Differing-phase rejection lives in vtree/events.ml (both consumers). Carry to Task 6: task-5-review.md's six Minors (docs/test-robustness) — take the cheap ones.

Task 6: complete (cd2f446 + f573799, re-review Approved). Critical fixed: get_selected_rows' stub wraps rows without ref_sink (transfer-container) → selection read via get_row_at_index + is_selected walk (also Live_tree), GC-churn live regression (segfaults on old code); binding defect recorded for Task 14 (twin of the FlowBox fix the fork carries). C1b (found by the implementer): Child_keys ephemeron must be keyed on the child widget the patcher retains, not the row wrapper — invariant documented in child_keys.mli; Tasks 7–8 inherit it. I1: ~selected filtered to keys with rows; ghost key inert until its row arrives, then selected that frame (same-frame rule, pinned both directions). Missing-key check at the constructor (Node.require_child_keys, retrofitted to Node.stack); duplicate keys stay at mount/patch with a path. Carry to Task 7: task-6-review.md N1–N3 (docs/cosmetics).

Task 7: complete (3d05a83 + e9e7793, re-review Approved). FlowBox getters verified ref_sink-safe in the stub; selection still read by walk. I1 fixed: apply_selection builds a per-call key→child map (24 ms → 0.39 ms per idle frame at 200-of-1000), in both w_flow_box and w_list_box, with a live bench regression. Plan correction recorded: GtkFlowBox.remove DOES emit selected-children-changed on 4.22 (the "documented quirk" was stale). Carry to Task 8: task-7-review.md N1 (raise bench bound_ms 2.0 → ~8.0 or ratio-based; false failure at 2× oversubscription) and N2 (two words).

Task 8: complete (0a4d11d + ced908d, re-review Approved, no Critical; I1 was doc-only). Notebook: keyed pages with Attr.tab_label (dropped → None), controlled current_page by key via the fixup queue (reads the live widget back), Move → reorder_child (direction-independent index), tab_pos via vtree/tab_position.ml, gallery page 6. GTK fact: set_current_page on a page whose child is hidden emits switch-page but leaves current page unchanged (documented). Lists bench bound now ratio-based (task-7 N1). Carry to Task 9: task-8-report.md "Carries to Task 9" (six items) + task-8-review.md N9 wording.

Task 9: complete (8848e2b + 1c61269 + d9f9d93, re-review 2 Approved). TextView: controlled buffer with a cached last-written text (no per-idle-frame buffer read; bench-pinned), cursor policy (character offset saved/restored, clamped) documented on Node.text_view; on_changed via the buffer object; invalid UTF-8/NUL text is refused before the write, buffer+cache untouched, reported once per distinct text via Patcher.ctx.report; parked refusal decided before any compare (R1). Carry to Task 10: task-9-report.md "Carries to Task 10" + reviewer's note: Patcher.ctx.report now has one caller — Task 8's hidden-page divergence and a list-box ~selected the mode cannot hold are the same shape and should get the hook (and their parked frames measured) in whichever task touches them; Live_tree truncates at 60 characters.

Task 10: complete (ce6497d + b863bb2, re-review Approved). DropDown: string-list model SPLICED in place (never replaced; String_list.new_ via the generator's ref_sink-on-GObject leaks one ref — backlog/fork note), controlled selected with reassert, notify::selected read-back spec with the index; an index beyond the items no longer raises at the constructor (still rejects < -1): refusal recorded, reported once via ctx.report, selected on the frame the items grow (same-frame rule); -1 over a non-empty list is a GTK no-op (GtkSingleSelection autoselect) handled the same way. LevelBar: bounds-before-value order is observable via GTK's offset CSS classes (pinned); negative bounds rejected at the constructor; Events.for_kind gained Level_bar. node.mli now states that a constructor Invalid_argument inside the computation breaks the driver. Carry to Task 11: task-10-review.md re-review N1–N3 (wording/report corrections).
User ruling 2026-08-30: the ocgtk fork MAY be pushed and Stavekeeper's ocgtk-pin.json bumped when Task 14's binding fixes are ready (no prior review required). Task 14's brief stays "prepare locally"; the controller pushes after Task 14's review.

Task 11: complete (179e70a + 2f8eeb9, re-review Approved). Calendar: controlled date written day-1-first (the plan's year/month/day order was wrong — measured, pinned); On_day_selected read-back connected to day-selected + notify::month + notify::year (spec machinery: connection list, one slot) with exact dedup against a last-fired memo, so heading walks reach the model (C1); marked_days outside 1–31 rejected at the constructor; year-0 date refused-recorded-reported. EditableLabel: text via W_entry machinery on the GtkEditable the widget is; editing controlled; create "" then reassert. Task 10 had missed adding drop_down/level_bar to test_gallery — fixed here. Carry to Task 12: task-11-report.md "Carries to Task 12 (updated)" + reviewer's note: the guard protecting a re-pick of a declined day is defended by a comment, not the test that names it — a few-line change to the existing block.

Task 12: complete (27a8b9c + 629185c, re-review Approved). Expert.embed: `~host` dropped (no use-after-free — wrapper refs); `?root_kind` on Driver.create; Embed owns a layout-transparent wrapper widget, re-parents the driver's root per frame on a root-kind change (C1), stop unparents the tree and leaves the wrapper, Driver.mark_broken public as the destroy backstop; Patcher.mount is now exception-safe (partial subtree destroyed on raise — closes the M1 backlog item) (I1); finaliser pins both directions (I2) turned up: Driver.stop must drop on_root_widget_changed, and the destroy backstop must be disconnected in stop (wrapper→closure→driver→callback→wrapper cycle; disconnecting is what makes the wrapper finalisable). N1 (milestone-level): the GC-finaliser re-entry is an ocgtk memory-safety bug (segfault when the callback allocates) — Global Constraints addendum committed (plan), rule to signals.mli in Task 13, binding fix in Task 14. Carry to Task 13: the `Embed.stop` comment sentence the reviewer drafted (closure cycle), task-12-review.md re-review Minors, and the rule in signals.mli.

Task 13: complete (c5fb8a2 + e7a1e7e, re-review Approved). Three sweeps (kinds vs constructors — compiler-counted; every Attr.Name.all name exercised — 19 were not, now placed observably; per-kind lifecycle). Gallery Input page (on_click/on_key/focus/embed) — the compensating control for the untestable input paths; four controlled props in the gallery were eating input, all state-backed now. Bonsai_gtk_test.Handle is a hand-written signature whose recompute_view / recompute_view_until_stable validate (Placement/Events/key-phase) — one existing test_handle case had been certifying an illegal tree. Carry to Task 15: task-13-review.md re-review N8, N9 (one sentence each). XTEST follow-up filed by the controller (bead below).

Task 14: implemented (fork branch m2-bindings 7619876c..4ea70268, pushed to GitHub as a branch; bonsai_gtk cc762d1). Review (task-14-review.md): NEEDS FIXES — C1 pango_glyph_item_apply_attrs's 4b free loop double-frees the aliased transfer-full instance (regression); I1 gtk_builder_get_objects (bare-GObject elements) missed by the sweep; I2 finaliser guard covers only finalize_gobject of three finalisers; I3 unbounded g_warning / fatal-warnings; I4 semantic change undocumented; I5 test comment wrong; I6 override action not in the maintainer reference. Fix round: agent m2-t14-fix (new commits on m2-bindings; pin NOT bumped until re-review approves). Server rebooted 2026-08-30 between the original implementer and the review; state recovered from CHECKPOINT-2026-08-30.md.

Task 14: COMPLETE. Fix round 1 (03ba87f8..cd071aa1) closed C1/I1-I6; re-review "Approved to push+pin". Fix round 2 (649498b4): the fatal-warnings dune rule broke the pin's hermetic nix build (GTK's own "Unable to acquire session bus" warning in the sandbox); the test now asserts the report's log level in-process instead. Fork m2-bindings pushed (head 649498b4) and fork main fast-forwarded to it. ocgtk-pin.json bumped to 649498b4 + task-14-pin-bump.patch applied and committed on m2; ci.sh all green against the pin. REMAINING from Task 14: the same pin bump in Stavekeeper (ocgtk-pin.json, flake.nix fetchFromGitHub hash sha256-0q4wCAXgYenhKwSobYJ1wVgs/ZptWeCp9UPipzTJ/Z0=, scripts/setup-ocgtk.sh; then nix build + smoke) — waiting for the settings agent to free that checkout; and dropping w_list_box's get_selected_rows prohibition comment now that the fix landed (fold into Task 15). Item 4d findings stay on the backlog for a later fork round.

Task 15: complete (055c70e, review "Approved with fixes" — no Critical; I1–I3 ride with Task 16): I1 spec §6.4 + signals.mli:76 claim three Payload signals, six ship (row-activated, child-activated, switch-page, pressed, key-pressed, key-released) — fix both, derive not copy; I2 README wrongly says CheckButton.set_group unbound; I3 Limitations omits the TextView/EditableLabel refuse-record-report rule. Plus task-15-review.md's Minors for Task 16 to take-or-argue. Backlog reconciliation verified.
Task 14 fully closed: Stavekeeper's ocgtk pin also bumped to 649498b4 (stavekeeper main 3bf997b; three placeholder call sites adapted; dune build/test, nix build and the full smoke green there).

Task 16: complete (53d13fb + 887e221 + 3cb9d09). Clean-tree gate: `git status` empty (the untracked `.beads/issues.jsonl` moved aside and restored), `git clean -ndx` reviewed and NOTHING cleaned with it — only `_build`, `_build.pkg`, `_build.pkgtest`, `result`, `bonsai_gtk.install` removed by hand; `.superpowers/`, `_opam/`, `.ocgtk-src/` untouched. `nix develop -c ./scripts/ci.sh` → **all green in 84 s, and green in three further clean runs (81 s each)**; both `-p` builds and the three-example smoke inside it; §7 verification by hand (37 widgets = 37 registry arms, 0 MISMATCH, all five controller attrs and Expert.embed real; `Events` is in vtree, `test/` takes only `bonsai_gtk.vtree`). Task 15 carries all taken: I1 (three→six `Payload` signals, in spec §6.4 AND signals.mli, with the generalisation stated — child-widget arguments + controller signals), I2 (CheckButton.set_group IS bound; reason replaced, advice kept), I3 (the TextView/EditableLabel refuse-record-report bullet, plus the Widgets table's contradicting claim), and all six Minors, including M2's code change to the `start`-side root message (golden promoted). Backlog's `git diff --exit-code HEAD -- '*.opam'` taken. **The run found a real gate defect:** `live_controllers.ml`'s focus block failed once — `focus-leave` before the `grab_focus` meant to cause it. Cause: one `xvfb-run` wraps the whole dune invocation, so all eleven live executables share one X display and run in parallel (`live_text` alone is 25 s of the section's 29, so it overlaps everything), and a neighbour's window mapping steals the input focus. Measured: 1/10 parallel, 0/15 serial, 0/15 solo, 2/8 with deliberate window churn. Fixed with `-j 1` (costs 2 s of 29); the two better fixes (an xvfb-run per rule; a focus block that does not depend on toplevel focus) are on the backlog. **Trap worth knowing: `dune build @test/live/runtest --force` does NOT re-run these rules** (each declares a target) — it returns in 3 s having run nothing and reports a flaky test green as often as you ask; delete `_build/default/test/live/output_*.txt` between runs. **Step 4 deviation: no real display (headless server), so the click-through was done under Xvfb with xdotool — the XTEST route the backlog calls its most valuable follow-up, tried for the first time and working.** Real X button presses and keystrokes into the gallery's Input page, screenshots read: button 1/2/3 each reported as itself, `press 2` on a double click, `ctrl` on a ctrl-held click, widget-local coordinates, `hello` typed with the keyval reported per keystroke, Escape counted and consumed while the entry kept its text, focus moving on Tab. First end-to-end exercise of `Attr.on_click`/`on_key_pressed` in the repo; still a demonstration, not a test. It found that the gallery's "click the card" instruction was wrong — the gesture is on the label and `Attr.margin` is outside a widget's allocation, so a click 14px below the words does nothing (which is why pass 1's right/double clicks looked like a broken click path); instruction corrected, the frame-vs-label choice filed. Artefacts: `task-16-report.md`, `task-16-ci-clean.log`, `task-16-clickthrough/` (scripts + 35 screenshots).

Task 16: implemented (53d13fb + 887e221 + 3cb9d09; clean-tree ci.sh all green ×6, 81-91 s; -p builds green). Click-through: first end-to-end exercise of on_click/on_key via XTEST/xdotool under xvfb — buttons 1/2/3, n_press, coords, modifiers, Capture consumes vs Propagate, focus enter/leave all proven on the real X input path; found the gallery's "click the card" instruction pointed at margin space (fixed; gesture-on-frame choice backlogged) and re-confirmed Task 13's controlled-prop fixes. Clean run flushed out a real race: all live executables shared one X display in parallel, so window mapping stole focus mid-test (measured 1/10 parallel, 0/15 serial) — live tests now run one at a time. Task 15's I1-I3 carries landed. Review: m2-r16 running. After approval: final whole-branch review split by area (core/controls/containers/tests per the plan's lenses), fix wave, re-review, merge m2→main, push.
Task 16 review: "Approved with fixes" (no Critical; I1 prose overclaims the refusal rule across five text widgets; I2 a stale backlog entry still calls the diagnosed focus race a timing flake with "do not fix" framing; I3 dune (locks) verified as the better serialisation mechanism — recorded as the third alternative, decision left to the final review). Fix round dispatched to m2-t16. FOR THE FINAL REVIEW/MERGE: (a) the click-through re-runs nowhere — the XTEST bead (bonsai_gtk-5qv) is the highest-value open item; decide at merge; (b) (locks x-display) vs -j1; (c) the entire SDD record is untracked via .git/info/exclude — decide at merge whether the milestone's evidence should survive the checkout (user decision); (d) whether the three entry widgets should refuse NUL like text_view/editable_label — backlog, weigh in the final review.

Task 16 fix round 1: complete (f06a615, ci.sh all green; review "Approved with fixes", no Critical, nothing in code). I1: Task 15's I3 fix had overshot — the README promised the refuse-before-write rule for all five text widgets; verified against the code and now stated per widget (text_view refuses NUL + invalid UTF-8; editable_label refuses NUL only and writes invalid UTF-8 on purpose, since a GtkEditable round-trips the bytes — its own comment says so in bold with the measurement; entry/search_entry/password_entry validate nothing, so a NUL truncates silently and the widget is rewritten every idle frame). Cache clause scoped to the text view. No widget code changed per the ruling; "should the three entries refuse a NUL?" filed under Do first in M3 for the final review. I2: the task-14 M11 backlog entry still called this task's own golden diff an accepted timing flake under a heading that says do not fix these — struck and rewritten in place (shared display, not timing; "if you see this again the serialisation has been lost"; cross-referenced both ways with the Plumbing item). I3: `(locks x-display)` added as the third and probably-best alternative in the report, ci.sh's comment and the backlog — it binds the constraint to the rules rather than to one flag on one call site (the reviewer reproduced the race through the invocation test/live/dune still permits) and does not serialise anything else; reviewer verified it on this toolchain (two 2 s rules at -j 8: sequential, 4.04 s). Not swapped in — eleven rules, no review round. test/live/dune's comment gained the warm-tree caveat for the 2 s figure. All seven Minors taken: two overstatements in the click-through's "what this proves" moved to "what it does not prove" (the Escape shows the key reached the capture-phase handler, NOT that Handled_and consumed it; the Tab cannot see on_focus_leave at all) and the same sentence fixed in the README; "not covered" → "no *test* covers"; 36 w_*.ml not 37 (the 37th registry arm is Native); backlog per-test floor 24 ms not 88; the Xvfb display-number reasoning in §8 was wrong (-a starts at :99, below the leaked servers); one loose blank line; montage-entries.png regenerated from tiles that exist in the archive. Reviewer's independent numbers folded in: 3 failures in 7 parallel runs on a loaded host (so 1-in-10 is a floor), 8/8 serial, --force confirmed vacuous at 3.257 s. Left for the final review, as the review asked: the XTEST bead's promotion, (locks) vs -j 1, and whether .superpowers/ (excluded via .git/info/exclude:7) should survive the checkout.

Task 16: complete (53d13fb + 887e221 + 3cb9d09 + f06a615, re-review Approved; its two trailing Minors fold into the final fix wave). ALL SIXTEEN TASKS DONE. Final whole-branch review dispatched: five lenses over 86224d9..f06a615 (packages final-{core,controls,containers,tests-headless,tests-live}.diff; final-docs.diff as shared reference); only the tests-live reviewer builds/runs gates. Merge-time decisions parked: XTEST bead at merge, (locks) vs -j1 in the fix wave, entry-NUL question, committing the .superpowers evidence (user).

Final review, four of five lenses in (reports final2-*-report.md):
- core: NEEDS FIXES — I1 apply_stack_claims raises outside mount's exception-safe region (presented window + orphaned tree; mli promises otherwise; three-line fix + mli sentences; the one core would not merge without); I2 destroy not exception-safe (mount's mirror; collect-and-reraise + Exn.protect in Driver.stop/Embed.stop, or backlog with reasoning); I3 events.mli/patcher.mli list 3 of 5 controller attrs (derive, don't list); I4 attr.mli/attr_apply.ml still blame the binding for the widget_name caveat the pin bump removed. Minors: M1/M2/M7 cheap.
- headless tests: APPROVE WITH FIXES — C1 the three "what the handle checks" claims are wrong three ways (fix = prose + the report's 16-row gap table; cheapest code closures in value order: root-kind check, duplicate-key check via existing check_unique_keys, window-below-root); I1 three event attrs have no Action + an action-coverage sweep; I2 two "declined" tests can't distinguish declining from never-running (follow the file's own better pattern); I3 Set_text accepts NUL/invalid-UTF-8 the runtime refuses (doc + gap table); I4 two "the one place" superlatives, five places (replace with the table); I5 per-package @runtest masks the sweeps (one README sentence; optionally move the vtree-only sweeps into test/).
- containers: APPROVE WITH FIXES — I1 moving a selected row NULLs GTK's container-side selection anchor while every library reader still says selected (fix in wave: unselect/select pair around the move in w_list_box+w_flow_box — plain re-select early-outs — plus a live assertion through GTK's own getters); I2 five sites claim "unnamed tab" where GTK draws a POSITIONAL "Page N" that renumbers on reorder (docs + a lead decision on writing Some "" vs doc-only; test_widgets' "measured" claim must be re-measured with show_tabs true); I3 stack ~visible_child has the notebook's hidden-page divergence undocumented. Minors N1-N4.
- controls: APPROVE WITH FIXES — I1 editable_label's ~editing lacks the "pair with the attr" sentence and is the worst omission (flashes into an entry and snaps back forever, silently); I2 refuse-record-report is one rule hand-copied four times (factor when the entry-NUL decision is taken); entry-NUL RECOMMENDATION: yes, refuse, in W_entry.set_text_if_needed so all four GtkEditable widgets share it (post-merge wave or M3 — controller to rule); Minors M1-M5 with an order.
Awaiting the tests-live lens, then ONE fix wave over the union.
- tests-live: APPROVE WITH FIXES — C1 ci.sh leaks BONSAI_GTK_LIVE_TESTS (an exported =1 makes the pure/headless steps run all eleven live suites in parallel and the real live step vacuous); C2 live_embed's bound_ratio 1.2 flakes at 2x load (sequential phases don't cancel contention; interleave or 2.0); I1 a second ci.sh in the same tree caches the live results (wall-clock asserts cached green); I2 the selection bench guards flow_box only; I3 the opam env eval can't fail the script. LOCKS RECOMMENDATION: swap -j1 for (locks x-display) on the nine toplevel rules — measured: cold 59s→33s, race 4/7 unserialised → 0 focus failures with locks, and it covers every invocation path incl. C1's leak. XTEST sketch written (report §"The XTEST bead") — merge decision.
CONTROLLER RULINGS FOR THE FIX WAVE (2026-08-30): take everything the five reports mark fixable now, specifically: core I1+I2 (exception safety both directions) and I3/I4 prose; headless C1 prose+gap table AND the three cheap code closures (root-kind, duplicate keys via check_unique_keys, window-below-root) + I1 three new Actions + coverage sweep + I2 strengthened declined tests + I4 table replaces superlatives + I5 README sentence (+ move vtree-only sweeps into test/ if small); containers I1 unselect/select repair + live assertion via GTK's own getters, I2 DOC-ONLY (no Some "" — stack precedent) with the "measured" claim re-measured under show_tabs, I3 documented; controls I1 sentence + M1-M5 + ENTRY-NUL TAKEN NOW: NUL refusal in W_entry.set_text_if_needed shared by all four GtkEditable widgets, and the four hand-copied refusal mechanisms factored into one shared module while adding it; live C1 env pinning in ci.sh, C2 interleave-or-2.0 (implementer picks, records), I1 un-cache the live step in ci.sh, I2 bench twin, I3 eval propagation, and the LOCKS swap. XTEST stays a merge decision (bead bonsai_gtk-5qv).

Fix wave: complete (8bd5df9..36aa26c, seven commits; disposition table in final2-fix-wave-report.md; both/three ci runs all green with the live step re-running). Scoped re-reviews: ALL FIVE LENSES APPROVED (core: both code findings fixed with live regressions; containers: I1 repair correct; controls: everything taken incl. entry-NUL in src/widgets/refusal.ml; headless: gap coverage 3→6 of 16 with honest wording, messages byte-identical; live: all six verified by re-running the finding experiments, locks swap in). M2 COMPLETE 2026-08-31: m2 merged --ff-only into main and pushed. Merge-time items handed to the user: XTEST re-runnable click-through (bead bonsai_gtk-5qv; sketch in final2-tests-live-report.md §"The XTEST bead"), committing the .superpowers evidence.
